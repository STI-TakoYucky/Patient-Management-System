package mvc.views;

import javax.swing.*;

public class App extends JFrame {
    public App() {
        setVisible(true);
        setSize(600, 600);
        setTitle("PatientManagementSystem");
        setLocationRelativeTo(null);
    }
}
